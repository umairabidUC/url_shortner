/** @type {import('next').NextConfig} */
const nextConfig = {reactStrictMode: true,
    images : {
      domains : ['sangw.in', 'localhost', 'picsum.photos','assets.aceternity.com'] // <== Domain name
    }};

export default nextConfig;
